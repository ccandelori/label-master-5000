#!/usr/bin/env bash
# Creates the sidecar's virtualenv and installs pinned dependencies.
set -euo pipefail
cd "$(dirname "$0")/.."

python3 -m venv .venv
.venv/bin/pip install --upgrade pip
.venv/bin/pip install -r requirements.txt
echo "ocr_service ready - start it with ocr_service/bin/serve"
