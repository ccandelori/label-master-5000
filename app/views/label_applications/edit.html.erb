<h1 class="text-2xl font-semibold mb-2">Edit application <%= @application.serial_number %></h1>
<p class="text-ink-muted mb-6 max-w-2xl">
  Saving revalidates the label against the same artwork. The original reading is
  reused, so the validation is instant and the earlier result stays in History.
</p>

<%= render "form", application: @application %>
